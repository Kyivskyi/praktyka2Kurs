# pip install -r requirements.txt

# GUI Framework
PySide6>=6.5.0         # Main GUI framework (with QTranslator for translations)
pylupdate6>=6.5.0      # For generating .ts translation files

# Document processing
pdfminer.six>=20220524 # PDF to TXT conversion
python-docx>=0.8.11    # DOCX file manipulation
PyMuPDF>=1.22.0        # PDF operations (fitz)
docx2pdf>=0.1.7        # DOCX to PDF conversion
pdf2docx>=0.5.6        # PDF to DOCX conversion

# Multimedia processing
ffmpeg-python>=0.2.0   # Audio/video conversion
Pillow>=9.3.0          # Image processing

# Data processing
pandas>=1.5.0          # CSV/Excel/JSON conversion